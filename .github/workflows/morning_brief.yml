name: Morning Media Brief

on:
  schedule:
    # 10:00 UTC = 6:00 AM EDT (summer) / 5:00 AM EST (winter).
    # If you want a strict 6 AM ET year-round, run two cron lines and gate
    # them with a date check, or use a workflow tool that supports TZ.
    - cron: "0 10 * * 1-5"
  workflow_dispatch: {}   # allow on-demand runs from the Actions tab

jobs:
  brief:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run brief
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
        run: python main.py
